# This file may be used to create an environment using:
# $ conda create --name envTeam8 --file environment.yml
# platform: linux-64
name: envTeam8
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - numexpr
  - pip:
      - gmsh
  - tensorflow=2.15